# 02 — Design

**STUB. Filled by S2 (#314), from S1's (#313) measured numbers.**

It is deliberately empty. The design-selection rule is pre-registered in `05_analysis_plan.md` §5, and
writing a design here before S1 measures would be choosing the answer and then collecting evidence for
it. The one thing fixed in advance is what *selects* the design, not the design itself.

S2 must record here: the noise family and why S1's numbers selected it; the accumulation scheme; the
numeric space and the non-negativity treatment; how the scale parameter maps to S1's measured error
magnitude; and the **deliberate fork** on whether the pushforward self-fed forward
(`training_engine.py:695`) is also noised.
