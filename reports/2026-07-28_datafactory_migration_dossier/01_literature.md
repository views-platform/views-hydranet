# 01 — Literature / prior art

This is an **engineering migration**, not a modeling program — the literature is thin. Prior art is
internal:

- **`project_datafactory_migration.md`** (memory) — the disciplines + the indicative cached parity.
- **`models/light_strider/configs/config_queryset.py`** — the datafactory descriptor template.
- **`blazing_meteor`, `bright_starship`** — arch twins (`HydraBNUNet06_LSTM4`) already on datafactory.
- **ADR-060** (static_channels — population seam), **ADR-047** (views-frames PF path), the
  **views-frames migration** (tasks #67–71).
- **`reports/2026-07-17_lodestar_eval_dossier`** — the viewser-era foundation being retired to v2.

**Gaps to fetch:** documentation of the datafactory's `africa_me_legacy` preprocessing (the source of
L1 `nokgi` and L2 vintage) — no doc found; derived empirically instead. The **population data
provenance** (which source/year the datafactory serves) — needed for P3 external validation.
