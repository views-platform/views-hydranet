# 01 — Literature / prior-art (seed; `/library` for external cites later)

Grouped by role; "what we take" noted. Fetch external cites via `/library search` before `promote`.

## In-house prior results (the binding evidence)
- **[[project_densemse_beats_baseline]]** — dense-mse+wBCE beats white_ranger (conflictology) on T=0 CRPS;
  loses QS99 via under-firing. *Take:* the front-runner + the exact weakness (bulk under-fires).
- **[[project_body_knob_quest]]** (survey 2026-07-16) — every body loss went TIMID or EXPLODED; the
  **winsorized-target** angle is the one untried door; `count_mean` is the only proven lifter (but OOS-
  collapsed, uncapped). *Take:* winsorize = the new variable that should stabilize a lifter.
- **[[project_volatility_ceiling_predictable]]** — bulk spread/level predictable; conditional quantiles
  calibrated. *Take:* the bulk IS learnable; the tail is risk-not-value.
- **[[project_amount_ceiling_wall]]** — extreme value irreducible from covariates. *Take:* park the tail.
- **[[project_gate_loss_finding]]** — `pos_weight` = the gate knob, proven. *Take:* the body should have an
  analog dial (τ).
- **[[project_t0_calibration_failure]]** — MCR pooled-mean is the wrong lens (hid the failure ×3). *Take:*
  per-cell `ratio_med`, never MCR.

## External anchors to fetch (gaps-to-fetch)
- Winsorized / trimmed M-estimation; robust regression on heavy tails.
- Expectile / asymmetric (pinball) regression as a mean-vs-median dial; Gneiting on consistent scoring.
- Frequency–severity + peaks-over-threshold splicing (actuarial) — the composite structure the program sits in.
- Zero-inflated + bulk-tail (extended GPD) count models.
